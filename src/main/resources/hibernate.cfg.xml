<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
        
<hibernate-configuration>
    <session-factory>

        <!-- CONEXIÓN MARIADB -->
        <property name="hibernate.connection.driver_class">org.mariadb.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mariadb://localhost:3307/streamingcatalog?serverTimezone=UTC</property>
        <property name="hibernate.connection.username">demo</property>
        <property name="hibernate.connection.password">demo123</property>

        <!-- DIALECTO -->
        <property name="hibernate.dialect">org.hibernate.dialect.MariaDBDialect</property>

        <!-- SCHEMA AUTO -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- LOG SQL -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- ENTIDADES -->
        <mapping class="es.dam.streamingcatalog.model.Estudio"/>
        <mapping class="es.dam.streamingcatalog.model.Pelicula"/>
        <mapping class="es.dam.streamingcatalog.model.Critica"/>

    </session-factory>
</hibernate-configuration>
